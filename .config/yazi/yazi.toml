[manager]
ratio = [1, 4, 3]
sort_by = "modified"
sort_reverse = true
linemode = "size"
show_hidden = true

[preview]
# Alacritty doesn't support graphics protocols natively
# Yazi will auto-detect and fallback to Chafa (ASCII art) if installed
max_width = 600
max_height = 900
